/* ============================================================
   FLOT — Constants
   ============================================================ */

/** Default direction for trips (arrivals) */
export const DEFAULT_DIRECTION = 'TO_MILAN';

/** Minimum touch target (Apple HIG) */
export const MIN_TOUCH_PX = 44;

export const GOOGLE_PLACES_API_KEY = import.meta.env.VITE_GOOGLE_PLACES_API_KEY as string;
